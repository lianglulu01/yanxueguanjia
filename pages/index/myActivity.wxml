<!--pages/index/m y.wxml-->
<view>
  <view wx:for="{{mylist}}" wx:key="index">
    <view>{{item.status_text}}</view>
    <view>去签到</view>
    <image src="{{item.pic_url}}"></image>
    <view>{{item.name}}</view>
    <view>{{item.people}}人报名</view>
    <view>时间:{{item.start_time}}-{{item.end_time}}</view>
    <view>立即查看</view>
  </view>
</view>
